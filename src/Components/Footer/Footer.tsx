import { Wrapper } from './Footer.styles';
import Box from '@material-ui/core/Box';
import Container from '@material-ui/core/Container';

const Footer = () => {
  return (
    <footer>
      <Wrapper disableGutters>test12</Wrapper>
    </footer>
  );
};

export default Footer;
